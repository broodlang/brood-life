# Brood package cache — regenerable from project.lock.blsp (which is committed)
_deps/

# Editor / OS cruft
*.swp
.DS_Store
.brood_crash_dump

# Runtime perf log (regenerated each run by perflog)
life.log

# nest release default output (binary is named after the project)
/foobar
